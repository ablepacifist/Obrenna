# Web framework
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# Data / validation
pydantic>=2.7,<3.0
SQLAlchemy>=2.0,<3.0
pandas>=2.2

# Agent orchestration
langgraph>=0.2,<1.0

# Local model adapter (OpenAI-compatible, local-first)
httpx>=0.27

# Artifact / PDF export
jinja2>=3.1
playwright>=1.45

# Hardware detection
psutil>=5.9

# Local embedding runtime (ONNX-backed, CPU-friendly)
fastembed>=0.3,<1.0

# MCP SDK (for future direct stdio/SSE transport)
# mcp>=1.0  # Uncomment when MCP SDK ecosystem is finalized

# Dev / test
pytest>=8.0
jsonschema>=4.21
